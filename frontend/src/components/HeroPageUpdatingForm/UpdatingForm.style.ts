import { styled } from '@mui/system';
import { Form } from 'formik';

export const StyledUpdatingForm = styled(Form)(() => ({
  width: '100%',
  display: 'flex',
  flexDirection: 'column'
}));

export const UpdatingFormDeleteButton = {
  mb: 5
};

export const UpdatingFormSaveButton = {
  mb: 5
};